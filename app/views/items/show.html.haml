.title
  = "#{@item.name}の初心者向けオススメ動画一覧"
.main
  .grid
    - @trainings.each do |training|
      %iframe{:allow => "accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture", :allowfullscreen => "", :frameborder => "0", :height => "300px", :src => "#{training.url}", :width => "100%"}